{
  "name": "trading-bot",
  "version": "1.0.0",
  "description": "Automated momentum trading bot with dashboard",
  "scripts": {
    "dev": "concurrently --names API,UI --prefix-colors cyan,magenta \"npm run api\" \"npm run frontend\"",
    "api": "python3 -m api.server",
    "frontend": "cd frontend && npm run dev",
    "install": "pip3 install -r requirements.txt && cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
